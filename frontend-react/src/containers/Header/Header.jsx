import React from "react";

export const Header = () => {
  return (
    <header id="home-header">
      <div className="container header-container">
        <div className="hero-container">
          <main>
            {/*TODO [menna] : implement hero make sure follow thus instructions
              hero should be responsive and follow layout tasks/hero
              each hero slide should contain title and paragraph
             */}
          </main>
        </div>
      </div>
    </header>
  );
};
